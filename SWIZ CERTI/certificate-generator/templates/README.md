# Swizosoft Certificate Generator

A Flask-based certificate generator that automatically places the user's name on a certificate template and downloads it as a PDF.

## How to Run

1. python -m venv venv  
2. source venv/Scripts/activate  
3. pip install -r requirements.txt  
4. python app.py  
5. Open: http://localhost:5000
